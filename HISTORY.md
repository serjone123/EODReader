# HISTORY.md — история изменений проекта

Этот файл ведётся всеми разработчиками и ИИ-агентами, работающими над EODReader.

## Правила

Каждая существенная правка должна иметь запись. Особенно обязательно фиксировать изменения, которые:

- меняют логику обнаружения пиков или классификации;
- меняют математические расчёты, фильтрацию, корреляцию, статистику или пороги;
- меняют формат входных/выходных данных или совместимость;
- меняют структуру конвейера обработки;
- исправляют баг, который мог влиять на научный/экспериментальный результат.

Для небольших механических правок достаточно одной строки. Для изменений логики нужна содержательная запись: **что изменено → зачем → что изменилось в поведении/расчётах → как проверено**.

Не переписывать историю предыдущих агентов. Новые записи добавлять сверху, сразу после раздела `## Записи`.

---

## Записи

### 2026-09-19 — Разделение GUI.Plot.pas на три модуля — opencode

- `GUI.Plot.pas` (2920 строк) разделён на `GUI.Plot.Base.pas`
  (`TPlotBase`, общие события `TViewChangedEvent`/`TOverviewClickEvent`/
  `TOverviewRangeSelectedEvent`, `EodChannelColors`), `GUI.Plot.Signal.pas`
  (`TSignalPlot`, `TPlotMode`, константы `MinViewSamples`/
  `MaxViewSamples`/`EnvelopeResolutionLimit`) и `GUI.Plot.Overview.pas`
  (`TOverviewPlot`). Код тел методов перенесён без изменений (механическое
  разделение), отличие только в заголовках юнитов и `uses`.
- Обновлены `ReadEOD.dpr`, `ReadEOD.dproj`, `uses` в `uReadWavMain.pas`
  (`GUI.Plot.Signal` + `GUI.Plot.Overview`) и `Video.OverlayRenderer.pas`
  (только `GUI.Plot.Overview`), таблица структуры в `README.md`.

### 2026-09-19 — Вынос оркестрации фоновых воркеров в GUI.Analysis.pas — opencode

- Новый модуль `GUI.Analysis.pas` — класс `TEodAnalysisController`: владеет
  четырьмя фоновыми воркерами главной формы (`Threads.Analysis`,
  `Threads.WavOpen`, `Threads.Overview`, `Threads.PeakOverview`), запускает
  их, отменяет и сбрасывает ссылку по завершении. Прогресс и результаты
  отдаются наружу событиями (`OnAnalysis*`, `OnOpen*`, `OnOverview*`,
  `OnPeakOverview*`); признак закрытия формы опрашивается через `Closing`, а
  завершившийся при закрытии воркер дёргает `OnCloseRequest`. Класс не знает
  ни о форме, ни о её контролах.
- `uReadWavMain.pas`: удалены поля `FAnalysis/FOpenThread/FOverviewThread/
  FPeakOverviewThread/FClosing`, методы `AnalysisThreadTerminated/
  OpenThreadTerminated/OverviewThreadTerminated/PeakOverviewThreadTerminated`
  и прямой код создания потоков. Добавлено поле `FBackground:
  TEodAnalysisController`, создаётся в `FormCreate`, события привязываются к
  существующим обработчикам формы. `FormDestroy`/`FormCloseQuery` работают
  через `FBackground.CancelAll`/`AnyRunning`; `StartAnalysis`, `StartOverview`,
  `StartPeakOverview` и обработчики кнопок вызывают методы контроллера. Из
  `uses` убраны больше не нужные `Threads.*`.
- Поведение сохранено: те же тексты статусов, порядок отмены, реакция на
  прогресс/ошибку/отмену, «список пиков не обновляется во время плея» и
  синхронизация списка при остановке. Реакция на данные (показ пиков, графики,
  `SetAnalysisUiState`) осталась в форме.
- Проверено: `cmd /c _build.cmd` — собирается (`2007 lines, 2.28 seconds,
  6023844 bytes code`), только известные `W1000 MessageDlg`; новых `H2219`
  нет. Поведение (закрытие формы с активным воркером, отмена анализа, обзор
  WAV/EODPK) проверяет serjone.

### 2026-09-19 — Вынос логики из uReadWavMain.pas в GUI.Playback и GUI.PeakList — opencode

- Новые модули подсистемы `GUI` (имена согласованы с serjone):
  - `GUI.Playback.pas` — класс `TEodPlayer`: воспроизведение записи как
    последовательности пиков. Сам владеет `TTimer` и `TStopwatch`; форма
    передаёт сессию и колбэки (показ пика, статус, текущий кадр, текущий
    индекс пика, позиция ползунка, состояние Play/Stop). Пересчёт стартовой
    позиции при смене скорости перенесён в `TEodPlayer.SetSpeed`.
  - `GUI.PeakList.pas` — класс `TEodPeakList`: страницы списка пиков
    (`FillFirstPage`, `FillAroundFrame` с бинарным поиском окна ±100 с,
    обработка ссылок `<<`/`>>` и правого клика). Держит только ссылку на
    `TListBox`; показ пика и запись в `edEndSample` — через колбэки.
- `uReadWavMain.pas`: удалены поля `FPlayTimer/FPlayActive/FPlayIndex/
  FPlayStartPos/FPlayClock/FPlaySpeed` и `FPeakListFirstIndex/FPeakListRealCount`,
  методы `StartPlayback/StopPlayback/PlayTimerTick` и
  `FillPeakList/FillPeakListAroundFrame/PopulatePeakListRange`.
  `FPlayButtonClick`, `PlaySpeedBoxChange`, `lbPeakListClick`,
  `lbPeakListMouseDown` стали тонкими делегатами в новые классы; убран
  неиспользуемый `System.Diagnostics`. Ссылки на сессию у плеера и списка
  обновляются в `OpenFinished` при замене сессии после открытия WAV.
- Поведение и математика не изменились: код перенесён 1:1 (немедленный показ
  первого пика, выбор стартового пика, пропуск промежуточных пиков за один
  тик, окно списка ±100 с, тексты статусов).
- Проверено: `cmd /c _build.cmd` — все модули компилируются (DCU для
  `GUI.Playback`/`GUI.PeakList` созданы), новых предупреждений нет. Финальная
  линковка не прошла из-за `F2039` (файл `Bin\ReadEOD.exe` занят запущенным
  приложением) — требуется закрыть приложение и пересобрать.
- Исправлен давний баг: во время воспроизведения `ShowPeak` на каждом пике
  перезаполнял `lbPeakList` (двигалась подсветка и подгружались новые окна
  пиков), что тормозило плей. Теперь показ пика обновляет список только
  когда плеер не активен (`if not FPlayer.Active`); при остановке плеера
  список один раз синхронизируется с последним показанным пиком через
  колбэк состояния. Проверено: собирается (`1835 lines`); поведение в плеере
  проверяет serjone.

### 2026-09-19 — Обзор EODPK: бинирование по кадрам; чистка uReadWavMain.pas — Claude

- `Threads.PeakOverview`: бакеты кэша теперь раскладываются по бинам обзора по
  позиции кадра (а не по индексу бакета), как в `Threads.Overview`. Раньше при
  неравномерной плотности пиков обзор не совпадал с красным прямоугольником.
- `Core.Types`: добавлена `BuildGeneralEnvelope` — общая огибающая выводится
  из поканальной (симметричная ±max|x| или min/max).
- Временно: переключатели «вид обзора» и «режим бакетов» на панели `LayNavi`
  для визуального сравнения вариантов; после выбора будут удалены.
- `uReadWavMain.pas`: удалены мёртвый `BuildOverview`, `ShowCurrentRange`,
  заглушка `AddMenuItem`, закомментированный код; переведены английские
  комментарии. Логика расчётов не менялась.
- проверено, есть нюансы:
  - Бакеты Диапазон отображает цветной овервью.
  - Бакеты центр отображает серый график, но какбудто состоящий из обсалютных значений и отзеркаленный в обе стороны
  - Обзор: серый ±max|x| и Обзор: серый min/max какбудто бы не отличаются между собой
  - Обзор: цвета каналов есть небольшие отличия для некоторых файлов, а какие-то выглядят одинаково.
- В итоге, оставляем пока переключатели, буду наблюдать дальше, но их состояние надо сохранять в настройках.

### 2026-09-19 — Перекодировка в UTF-8 с BOM, синхронизация документации — Claude

- serjone перекодировал исходники и `.md` в UTF-8 с BOM (ранее `Signal.Peaks.pas`
  и `TODO.md` были в cp1251); в `AGENTS.md` обновлено правило про кодировки.
- В `uReadWavMain.pas` удалена неиспользуемая заглушка `AddMenuItem`.
- В `README.md` исправлены устаревшие имена модулей, добавлены `Threads.Base`,
  `Threads.Overview`, `Threads.PeakOverview`, `Threads.VideoExport`,
  `GUI.VideoExportForm`, `GUI.FileNaming`, `Video.*`.
- `Threads.Base.pas`: вызов `DoFinished` снова защищён `try/except` (см. запись
  от 2026-09-18, в коде защиты не оказалось).
- Логика и расчёты не менялись. Собралось, работает.

### 2026-09-18 — Перевод Threads.Overview и Threads.PeakOverview на TEodBackgroundThread; доработка Threads.Base — Claude

- `Threads.Overview.pas` и `Threads.PeakOverview.pas` переведены на
  `TEodBackgroundThread`: собственные `FCancelEvent`, `FCanceled`,
  `FErrorText`, `Cancel`, `CancelRequested` и деструктор удалены, `Execute`
  заменён на `RunTask`, проверки отмены — на `CheckCancel`, ранние `Exit`
  с текстом ошибки — на `raise Exception`. Освобождение источника
  (`Source`/`Store`) по-прежнему происходит до вызова `DoFinished`.
- `Threads.Base.pas`: `Execute` перенесён в `protected` (serjone); вызов
  `DoFinished` защищён `try/except` — исключение из обработчика владельца
  пробрасывается в главный поток через `TThread.Queue`, а не теряется в
  `FatalException` потока; `raise EAbort.Create('')` заменён на `Abort`.
- Математика построения обзорных огибающих не менялась.
- Проверено, собирается, запускается.
- Автор: Claude (по запросу serjone).

### 2026-09-18 — Перевод Threads.Analysis и Threads.VideoExport на TEodBackgroundThread — Claude

- `Threads.Analysis.pas` и `Threads.VideoExport.pas` переведены на
  `TEodBackgroundThread`: собственные `FCancelEvent`, `FCanceled`,
  `FErrorText`, `Cancel`, `CancelRequested` и деструктор удалены,
  тело `Execute` заменено на `RunTask`, финальный колбэк — на `DoFinished`.
- В `Threads.Analysis` удалён `DoTerminate` (только вызывал `inherited`).
  Отмена детектора по-прежнему идёт через колбэк `DetectorCancel` →
  `CancelRequested`; при отмене результат анализа форма отбрасывает,
  как и раньше.
- В `Threads.VideoExport` ветки `Exit`/`FCanceled := True` заменены на
  `CheckCancel` и `raise Exception.Create(...)`; ошибка этапа B теперь
  доставляется через исключение, `FErrorText` заполняет базовый класс.
- Математика и алгоритмы обработки не менялись. Публичный интерфейс потоков
  для формы не менялся.
- Проверено, собирается, запускается.
- Автор: Claude (по запросу serjone).

### 2026-09-18 — Базовый класс для фоновых потоков

- Добавлен модуль `Threads.Base.pas` с классом `TEodBackgroundThread`.
  Каркас берёт на себя общий boilerplate воркеров: событие отмены
  (`Cancel`/`CancelRequested`), поля `FCanceled`/`FErrorText`,
  метод `CheckCancel` (бросает `EAbort`), гарантированный вызов
  `DoFinished` из `finally` при любом сценарии завершения
  (нормальное, `Exit`, отмена, исключение). Раньше `DoFinished`
  не вызывался при `Exit` и форма оставалась в состоянии «идёт работа».
- `Threads.WavOpen.pas` переведён на `TEodBackgroundThread`: тело
  `Execute` заменено на `RunTask`, россыпь `if CancelRequested then
  ... Exit` — на `CheckCancel`. Поведение не изменилось.
- Автор: DeepSeek (по запросу serjone).

### 2026-09-18 — Чистка мёртвого кода в GUI.Plot.pas + общий предок TPlotBase для TOverviewPlot/TSignalPlot — Claude

- Удалены крупные закомментированные дубли в `GUI.Plot.pas`: старая версия `TSignalPlot.ClampView`, старая версия `TSignalPlot.ZoomAt`, старый вариант агрегации в `DrawEnvelope4Channels` (заголовки "NORMAL MODE"/"REDUCED RESOLUTION MODE" сохранены и объединены с уже существующим пояснением про агрегацию по индексу), полный закомментированный дубль `TOverviewPlot.PaintBoxPaint` (~200 строк, дублировал `RenderPlot`, ничего уникального не содержал).
- Комментарий про семантику `TChannelEnvelopes` (`Поканальные min/max огибающие; индекс 0..3 — канал 1..4.`), ранее висевший над закомментированным дублирующим объявлением типа в `GUI.Plot.pas`, перенесён к актуальному объявлению `TChannelEnvelopes` в `Core.Types.pas` — там этого пояснения раньше не было.
- Введён общий предок `TPlotBase` для `TOverviewPlot` и `TSignalPlot`: владение `FPaintBox`/`FPopupMenu`, абстрактный `RenderPlot(Canvas, ARect)`, общий `PaintBoxPaint`, общий `RenderToBitmap(AWidth, AHeight)`/`GetPlotAsBitmap`, общее контекстное меню по правому клику (`Copy` + `Save to file...`).
- `TOverviewPlot` впервые получил контекстное меню (раньше правый клик по обзорному графику не обрабатывался вообще).
- `TSignalPlot.SaveToFile` (был объявлен, но нигде не вызывался) заменён на подключённый к меню `TPlotBase.SaveViewToFile` — пункт «сохранить текущий вид графика в файл» из TODO.md (`P0 — открытие файлов и GUI`) теперь реально доступен для обоих графиков, не только Copy.
- **Изменение поведения рендера (не логики анализа):** `TSignalPlot.RenderPlot` раньше игнорировал параметр `ARect`, всегда считая геометрию от `FPaintBox.Width/Height`. На экране это не проявлялось (`ARect` там и так совпадал с `FPaintBox`), но ломало бы `RenderToBitmap` для любого размера, отличного от экранного. Теперь везде используется `ARect` — это нужно, чтобы `RenderToBitmap`, унаследованный от `TPlotBase`, можно было в будущем использовать и для `TSignalPlot` (покадровый экспорт анимации основного графика), по аналогии с тем, как `TOverviewPlot.RenderToBitmap` уже используется в `Video.OverlayRenderer.pas`.
- Код не собирался и не проверялся `_build.cmd` (нет доступа к Delphi-компилятору у агента) — сборка и ручная проверка (правый клик на обоих графиках → Copy/Save to file; масштабирование/панорамирование основного графика; экспорт видео полосы через `btVideoExport`) остаются за пользователем.
- Автор: Claude (по запросу serjone).

### 2026-09-16 — Первая версия наложения обзорной полосы на видео эксперимента — Claude

- Добавлена подсистема `Video.*`: `Video.FfmpegLocate.pas` (поиск/хранение
  пути к ffmpeg), `Video.OverlayRenderer.pas` (headless-рендер кадров
  обзорной полосы через `TOverviewPlot`), `Video.FfmpegExport.pas`
  (ffprobe-детект параметров видео, кодирование кадров через stdin,
  ffmpeg overlay).
- Добавлен `Threads.VideoExport.pas` — фоновый воркер двухэтапного
  конвейера (рендер полосы → overlay-композитинг) с прогрессом/отменой,
  по образцу `Threads.Overview.pas`.
- Добавлена `GUI.VideoExportForm.pas` — немодальная форма с полями
  Offset и превью-длительности, кнопками Preview/Full/Отмена; кнопка
  вызова — `btVideoExport` на главной форме.
- В `GUI.Plot.pas` `TOverviewPlot.PaintBoxPaint` разделён на
  canvas-независимый `RenderPlot(Canvas, ARect)` и тонкую обёртку,
  плюс добавлен `RenderToBitmap` — по прецеденту, уже применённому в
  `TSignalPlot.RenderPlot`/`GetPlotAsBitmap`.
- Модель синхронизации: `VideoTime = SampleTime + OffsetSec`, Offset —
  одно число, подбирается пользователем вручную через Preview-рендер
  короткого отрезка перед полным экспортом.
- Первая версия: белый непрозрачный фон полосы (без альфа-канала),
  внешний вид совпадает с экранным обзорным графиком, добавлена подпись
  текущего времени WAV. Настройка внешнего вида/положения — отдельная
  задача (см. `TODO.md`).
- Код не собирался и не проверялся `_build.cmd` (написан без доступа к
  Delphi/Windows-компилятору) — сборка и ручная проверка на реальном
  видео остаются за пользователем. Особо неувереннен в порядке байт R/B
  при чтении `TBitmapData` в `Video.OverlayRenderer.BitmapToRgb24` —
  первое место для проверки, если цвета на итоговом видео выйдут
  перепутаны местами.
- Автор: Claude (по запросу serjone).

### 2026-09-14 — Обновление документации: электродная геометрия, конвенция именования, статус тестирования

- В `AGENTS.md` добавлен раздел «Конвенция именования модулей».
- В `README.md` добавлен раздел про подсистему `Electrode.*` (геометрия,
  разметка, локализация), раздел «Тестирование» с честным статусом, обновлена
  таблица структуры проекта.
- В `TODO.md` добавлен раздел «P1 — электродная геометрия и локализация
  рыбы» и уточняющая заметка в разделе про тестируемость.
- Изменений кода/расчётов эта запись не описывает.
- Автор: Claude (по запросу serjone).

### 2026-09-14 — Переименование `Source.Localization.pas` → `Electrode.Localization.pas`

- Модуль локализации рыбы (LM-оптимизация по 4-канальным амплитудам,
  `LocalizeMultiStart`), ранее ошибочно названный `Source.Localization.pas`,
  переименован в `Electrode.Localization.pas` — для соответствия конвенции
  именования `Подсистема.Назначение.pas` (зафиксирована в `AGENTS.md`).
- Обновлены ссылки в `ReadEOD.dpr` и `ReadEOD.dproj`.
- Логика модуля не менялась.
- Автор: serjone.

### 2026-09-14 — Устранение дублирующего мёртвого кода контекстного меню

- В `uReadWavMain.pas` удалён неработающий код контекстного меню
  (`PopupMenuImg`, обработчик `PaintBoxMouseDown` формы, `PopupMenuImgPopup`,
  `CopyIMG`): `TSignalPlot.Create` в `GUI.Plot.pas` перезаписывает
  `OnMouseDown` того же `PaintBox` уже после загрузки `.fmx`, поэтому
  обработчик уровня формы не выполнялся, а `TMainForm.CopyIMG` была пустой
  заглушкой.
- Рабочая реализация копирования графика в буфер обмена остаётся единственной
  — в `TSignalPlot`/`TOverviewPlot` (`GUI.Plot.pas`), со своим
  `PopupMenuImg` и `CopyIMG` через `IFMXClipboardService`.
- Уточняет запись от 2026-09-13 «Добавлены модули разметки электродов —
  serjone с гугом гемини»: меню было исправлено на уровне формы, что
  оказалось нерабочим путём; правильная реализация всегда была на уровне
  `GUI.Plot.pas`.
- Проверено сборкой (`_build.cmd`).
- Автор: serjone.

### 2026-09-13 — Добавлены модули разметки электродов — serjone с гугом гемини

- добавил доделал контекстное меню с копированием картинки

### 2026-09-13 — Добавлены модули разметки электродов — Claude

- Claude сгенерировал Electrode.Geometry.pas,
  Electrode.Layout.pas, Electrode.LayoutForm.pas
  и Electrode.Localization.pas.
- Добавлена самостоятельная форма для разметки углов
  аквариума, пар электродов и отметок рыбы на изображении.
- Реализованы геометрическая калибровка, относительные
  координаты, перевод в сантиметры и JSON Import/Export.
- Интеграция с основной формой и связь с текущими каналами
  записи пока не завершены.
- Автор: Claude.

### 2026-09-13 — Уточнение семантики peak records EODPK v3 — Codex

- В `EODPK_FORMAT.md` зафиксировано, что порядок `Position` является следствием последовательного прохода исходного WAV: штатно сформированные пики идут по возрастанию, поэтому полная проверка сортировки при каждом открытии файла не требуется.
- Зафиксировано, что окна waveform соседних пиков могут перекрываться.
- Перекрытия не считаются ошибкой, не устраняются при сохранении и не дедуплицируются при построении envelope cache: каждый пик хранит и агрегирует собственное окно независимо.
- Отмечено, что будущие расчёты по уникальным временным отсчётам должны отдельно определить политику обработки перекрытий, чтобы избежать двойного учёта.
- Автор: Codex.

### 2026-09-12 — Документирование EODPK v3 — Codex

- Добавлен `EODPK_FORMAT.md` как отдельная рабочая спецификация бинарного формата `.eodpk` версии 3.
- Зафиксированы логическая раскладка файла, header, fixed-size peak records, raw waveform вокруг пиков, многоуровневый envelope cache, правила прямого доступа и поиск диапазона без глобального массива `FPeakPositions`.
- Отдельно зафиксировано важное различие между raw waveform для точного просмотра и envelope cache для больших диапазонов: envelope не является заменой исходному сигналу для STD/FIR.
- В документе также перечислены инварианты, требования к RAM, проверки для больших файлов и идеи, которые пока не входят в v3.
- Описание сопоставлено с текущим `IO.PeakStore.pas` и `Core.Types.pas`; форматная реализация пока не изменялась.
- Обнаруженные при сопоставлении моменты для дальнейшей проверки оставлены в `TODO.md`, чтобы не менять бинарный формат без тестов и подтверждения поведения.
- Автор: Codex.

### 2026-09-12 — Фоновые workers для открытия WAV и построения Overview — Codex

- Открытие WAV выполняется через `Threads.WavOpen.pas` в отдельном worker: тяжёлая работа не блокирует UI, предусмотрены прогресс, отмена и передача готовой `TEodGuiSession` в главный поток.
- Расчёт Overview для WAV вынесен в `Threads.Overview.pas`; worker независимо читает четыре канала, считает общую и поканальные min/max-огибающие и передаёт готовые массивы в UI.
- Расчёт Overview для EODPK вынесен в `Threads.PeakOverview.pas`; worker использует существующий envelope cache `.eodpk`, не перечитывая исходный аудиосигнал.
- Исправлено применение результатов EODPK Overview: после завершения worker UI теперь передаёт рассчитанные массивы в `FOverview.SetData` и `FOverview.SetChannelData`, а не только меняет диапазон просмотра.
- Исправлены ошибки lifecycle при отмене/закрытии формы и timing обновления состояния кнопки `Open WAV`.
- Проверено пользователем на больших WAV и EODPK: интерфейс больше не зависает при построении Overview; после исправления результаты отображаются корректно. Пользователь отметил, что EODPK Overview также стал работать быстрее.
- Автор: Codex.

### 2026-09-12 — Автопоиск пары WAV и имя EODPK — Codex

- Добавлен `GUI.FileNaming.pas` с логикой распознавания суффиксов `_Tr12`/`_Tr34`, поиска имени парного WAV и формирования рекомендуемого имени `.eodpk`.
- При открытии WAV приложение автоматически предлагает парный файл; если он не найден, пользователь получает предупреждение и может выбрать файл вручную.
- Для пары вроде `ZOOM0005_Tr12.wav` / `ZOOM0005_Tr34.wav` в диалоге сохранения предлагается `ZOOM0005.eodpk`.
- Изменения подключены к GUI и проверены пользователем компиляцией.
- Автор: Codex.

### 2026-09-12 — Асинхронный worker для Overview — Codex

- Добавлен `Threads.Overview.pas`: расчёт обзорной огибающей WAV выполняется в отдельном `TThread` с независимым `TFourChannelAudioSource`.
- Worker считает общую min/max-огибающую и поканальные min/max для четырёх каналов, чтобы UI не обращался к `FSession` из фонового потока.
- Добавлена отмена и прогресс; применение готовых массивов выполняется только в главном потоке.
- В `Core.Types.pas` добавлен общий тип `TChannelEnvelopes`.
- Автор: Codex.

### 2026-09-12 — Оптимизация открытия вав файлов - chatgpt
 - При открытии первого файла, автоматически производится поиск второго 

### 2026-09-12 — Восстановление позиций пиков в GUI

- В `GUI.Model.pas` восстановлен метод `PeakPositions` для режима `dmPeakFile`.
- Позиции теперь читаются через `TEodPeakStore.ReadRecordInfo`, а не возвращаются пустым массивом из незавершённого lazy-cache кода.
- Добавлена защита от переполнения при преобразовании `PeakCount: Int64` в размер динамического массива.
- Зафиксировано, что публичный метод `PeakCount: Integer` всё ещё требует отдельного пересмотра для полной поддержки очень больших файлов.
- Проверка компиляцией ещё не выполнена: согласно `AGENTS.md`, сборку должен выполнить пользователь.

### 2026-09-12 — Исправление диапазона корреляции

- В `Detection.Correlation.pas` исправлен диапазон перебора позиций шаблона: теперь проверяются все допустимые нулевые позиции `0..N-L`.
- Исправлена инициализация результата для случая, когда лучший кандидат находится в позиции `0`.
- Это устраняет пропуск последнего допустимого окна и делает обработку первой позиции явной.
- Regression test пока не добавлен; он остаётся отдельным пунктом в `TODO.md`.

### 2026-09-12 — Изменения после работы других агентов

- Выполнено переименование модулей: все `.pas` остаются в одной папке, имена отражают подсистемы (`Core.*`, `IO.*`, `Signal.*`, `Detection.*`, `GUI.*`, `Threads.*`).
- `Signal.Peaks.pas`: прежний O(N²) расчёт prominence заменён на два прохода с монотонным стеком, заявленная семантика сохранена.
- `Detection.Detector.pas`: исправлен FIR15 padding с 7 до 8 отсчётов с каждой стороны чанка через `Fir15HalfWidth`; убраны занулённые первый/последний отсчёты каждого блока `ChunkSize`.
- Для новых изменений необходимы regression/golden-тесты: сравнение нового prominence со старой реализацией и проверка корректности FIR15 на границах чанков.

### 2026-09-12 — Инициализация журнала

- Создан `HISTORY.md` для координации работы нескольких разработчиков и ИИ-агентов.
- Зафиксировано требование документировать изменения логики обработки и расчётов.
- Добавлен `TODO.md` с планом стабилизации, тестирования, переименования модулей и дальнейшего развития.

### 2026-09-12