# TODO.md — план развития EODReader

Этот файл — рабочий план проекта. Все пункты имеют приоритет и статус.
После выполнения пункт переносится в раздел **Выполнено** в конце файла с краткой пометкой о результате. Если выполнение частичное — пункт остаётся в активном разделе, а прогресс фиксируется в `HISTORY.md`.

Статусы: `[ ]` не начато, `[~]` в работе, `[x]` выполнено.

## Правила работы для ИИ-агентов

Перед изменением кода агент обязан прочитать:

1. `README.md` — архитектура, форматы и общие сведения.
2. `AGENTS.md` — обязательные правила работы с проектом.
3. `TODO.md` — текущий план и уже выполненные пункты.
4. `HISTORY.md` — историю существенных изменений.

После существенного изменения агент должен добавить запись в `HISTORY.md`: что изменено, зачем, какие части логики/расчётов затронуты и как проверено. Особенно обязательно это для изменений математических расчётов, алгоритмов обнаружения/классификации и форматов данных.

---

## P0 — открытие файлов и GUI

- [x] Показывать постоянную информацию об открытом файле: имя/путь, формат, sample rate, число каналов, число сэмплов или длительность.
- [x] Автоматически искать второй WAV: `*_Tr12.wav` → `*_Tr34.wav`; при отсутствии предупредить и предложить ручной выбор.
- [x] Для пары `ZOOM0005_Tr12.wav`/`ZOOM0005_Tr34.wav` предлагать базовое имя сохранения `ZOOM0005.eodpk`.
- [x] Добавить в контекстное меню копирование текущего графика в буфер обмена и сохранение текущего вида графика в файл.
- [x] Устранить фриз интерфейса при открытии больших WAV: перенести тяжёлые операции из UI-потока, сохранить прогресс и отмену.
- [x] Устранить фриз интерфейса при открытии больших EODPK: расчёт Overview выполнять в фоне с прогрессом и отменой.
- [x] Перевести остальные фоновые потоки на `TEodBackgroundThread`:
      - [x] `Threads.PeakOverview.pas` — `TEodPeakOverviewThread`
      - [x] `Threads.Overview.pas` — `TEodOverviewThread`
      - [x] `Threads.Analysis.pas` — `TEodAnalysisThread`
            (проверить взаимодействие с существующим `DoTerminate`
            и колбэками детектора)
      - [x] `Threads.VideoExport.pas` — `TEodVideoExportThread`
            (два этапа: render bar + composite; вынести в `RunTask`
            оба этапа и сохранить `FStage`/`FDone`/`FTotal`)

## P0 — стабилизация и фиксация семантики

- [x] Исправить off-by-one в `Correlation`; regression test ещё нужен.
- [x] Определить полярность корреляции: сейчас используется `Abs(correlation)`.
- [ ] Зафиксировать семантику позиции события.
- [ ] Определить правило классификации одного кандидата.
- [ ] Переработать подавление близких событий.
- [x] Исправить `TEodGuiSession.GetPeakPositions` для `dmPeakFile`.
- [ ] Убрать опасное сужение `Int64 -> Integer`.
- [ ] Усилить валидацию WAV.
- [ ] Удалить устаревший закомментированный код.

## P0 — тестируемость и математическая корректность

> Статус на 2026-09: полноценного regression/unit-тестирования в проекте нет.
> Ранее отдельные тестовые программы (`Eod.Tests`, `Eod.PeakTest`,
> `Eod.IpiTest`, `Eod.ExtractTest`, `TestPeakStoreFast`) использовались для
> проверки отдельных концепций до сборки основного проекта; актуальность
> относительно текущего кода не подтверждена. Открытый вопрос — нужны ли
> тесты вообще, в каком объёме, какой инфраструктурой и как встраивать в
> процесс (`_build.cmd` сейчас тесты не запускает). Требует решения с
> пользователем.
- [ ] Создать unit/regression-тесты для основных модулей.
- [ ] Создать golden test для FIR15.
- [ ] Создать end-to-end тест с эталонными результатами.
- [ ] Зафиксировать математическую модель детектора.
- [ ] Принять решение: нужны ли автоматические тесты, в каком объёме и какой
      инфраструктурой (см. заметку выше).

## P1 — явная модель конвейера

- [ ] Ввести `CandidatePeak`, `Classification`, `Event`.
- [ ] Сделать явным конвейер `RawSignal → FeatureSignal → CandidatePeaks → ClassifiedEvents → DeduplicatedEvents`.
- [ ] Развести данные пика и классификации.
- [ ] Определить единую стратегию сортировки и дедупликации.

## P1 — работа с длинными записями

- [ ] Перевести `AnalyzePeaks` на `TFloatSignalCache` и потоковую/чанковую обработку.
- [ ] Добиться потребления RAM, не зависящего линейно от длительности записи.
- [ ] Добавить заголовок в `SignalCache`.
- [ ] Проверить границы сегментов при переходе между чанками.

## P1 — конфигурация и воспроизводимость

- [ ] Перевести окна обработки в физические единицы, если это не ломает совместимость.
- [ ] Версионировать конфигурацию и алгоритмы.
- [ ] Показывать причину отката к значениям конфигурации по умолчанию.
- [ ] Сделать результаты анализа воспроизводимыми.

## P1 — архитектура фоновых задач

- [x] Сравнить `Threads.WavOpen.pas`, `Threads.Overview.pas` и `Threads.PeakOverview.pas`: поля, lifecycle, отмену, progress/finished callbacks и обработку ошибок.
- [x] Вынести общую инфраструктуру background thread (cancel, progress, finished/error, lifecycle), не объединяя разные задачи в один универсальный thread с `case`.
- [ ] Рассмотреть общий слой источника данных для Overview (`WAV` и `EODPK`) и отдельный calculator, сохранив разные алгоритмы чтения данных.
- [ ] После рефакторинга удалить дублирование только после проверки сборки и тестов на большом WAV и большом EODPK.

## P1 — EODPK v3: спецификация и проверка реализации

- [x] Создать отдельную спецификацию `EODPK_FORMAT.md` для бинарного формата `.eodpk` v3 и связать её с `IO.PeakStore.pas`.
- [ ] Сопоставить спецификацию v3 с полной реализацией `IO.PeakStore.pas`: запись, чтение, cache offsets, level metadata и выбор уровня.
- [ ] Добавить regression-тесты формата: маленький/пустой EODPK, большой PeakCount/TotalFrames, random-access peak record и чтение envelope без WAV.
- [ ] Проверить, что `ReadEnvelope` действительно ограничивает RAM viewport-данными и не восстанавливает полный raw регион.
- [ ] Проверить инварианты v3 на реальном большом EODPK: сортировка `Position`, `RecordSize`, `SampleCount`, `CacheLevelCount`, `LevelCount`, `Offset`, `Count`, `GroupSize`.
- [ ] После тестов решить, нужно ли менять формат/reader; не менять бинарный layout без подтверждённого расхождения.

## P1 — электродная геометрия и локализация рыбы

- [ ] Завершить интеграцию `Electrode.*` с основной формой: связать разметку
      с текущими открытыми каналами записи.
- [ ] Реализовать автоматическое определение сопоставления канал↔электрод и
      полярности (+/-) через перебор перестановок по реальным амплитудным
      данным.
- [ ] Явно задокументировать и по возможности смягчить симметричную
      неоднозначность локализации (сейчас — мультистарт, физическая
      неоднозначность не устраняется полностью).
- [ ] Собрать набор реальных (не синтетических) калибровочных/
      локализационных данных и перепроверить точность на них; синтетическая
      проверка на случайных данных уже проводилась, но не сохранена как
      воспроизводимый тест.
- [ ] Решить, где хранить результаты разметки электродов и меток рыбы
      относительно сессии записи/файла `.eodpk`.
- [ ] Regression-тесты для `Electrode.Geometry.pas`/`Electrode.Localization.pas`,
      как только определится общий подход к тестированию проекта (см. раздел
      «P0 — тестируемость и математическая корректность»).
      
## P2 — качество, формат, производительность и GUI

- [ ] Исследовать адаптивный порог median/MAD.
- [ ] Добавить дополнительные признаки и confidence/quality score.
- [ ] Вынести шаблоны в версионируемые внешние файлы.
- [ ] Определить судьбу синтетического `Stim`.
- [ ] Развить `.eodpk` в воспроизводимый формат с версией алгоритма и конфигурацией.
- [x] Перевести `FindPeaksProminence` на O(N); нужны regression-тесты.
- [ ] Профилировать template correlation.
- [ ] Ограничить частоту UI progress updates.
- [ ] Сделать отмену отзывчивой внутри длительных операций.
- [ ] Упростить `GuiModel` после стабилизации ядра.
- [ ] Убедиться, что GUI использует единый API событий.
- [x] Допилить GUI.Plot в плане контекстного меню. По рекомендации гемини, сделать базовый класс с реализацией меню, от него наследовать овервьюплот и сигналплот


## P2 — наложение обзорного графика на видео эксперимента
> **Статус на 2026-09-16**: первая версия реализована Claude (см.
> `HISTORY.md`) — код написан, но не собирался и не проверялся сборкой
> (нет доступа к Delphi-компилятору у агента). Следующий шаг —
> `cmd /c _build.cmd` и ручная проверка на реальном видео/WAV.

> Идея: генерируется отдельное видео с движущейся полосой обзорного графика
> (как при воспроизведении в программе), синхронизированное по времени с
> видео эксперимента, и накладывается на него через ffmpeg. Видео эксперимента
> и исходный WAV не гарантированно синхронны по времени старта записи —
> нужен параметр смещения (offset).

- [x] Согласовать с пользователем новый префикс подсистемы `Video.*`
      (наложение видео не укладывается в `IO.*`/`GUI.*`, см. `AGENTS.md`,
      раздел «Конвенция именования модулей» — новый префикс нельзя вводить
      без согласования).
- [ ] Вынести отрисовку `TOverviewPlot` в canvas-независимый метод
      (`RenderTo(Canvas, Rect, ...)`), по образцу уже существующего
      `TSignalPlot.RenderPlot`/`GetPlotAsBitmap` — нужен для рендера кадров
      без реального `TPaintBox`.
- [ ] Добавить режим отрисовки с прозрачным фоном (сейчас `PaintBoxPaint`
      всегда заливает белым) — для наложения нужен альфа-канал.
- [ ] Определить и зафиксировать модель синхронизации: единственный скаляр
      `Offset` (секунды), знак и формула (`VideoTime = SampleTime + Offset`
      или наоборот) — зафиксировать в документации подсистемы.
- [ ] Реализовать генерацию кадров обзорной полосы в разрешении/частоте
      кадров видео эксперимента (через ffprobe/ffmpeg -i), потоково —
      без промежуточных PNG-файлов, если получится пробросить кадры в
      ffmpeg через pipe (`-f rawvideo -pix_fmt rgba`).
- [ ] Реализовать вызов ffmpeg для наложения (`overlay` filter), сборка
      команды и парсинг прогресса из stderr.
- [ ] Фоновый worker (`Threads.*`) для рендера+ffmpeg с прогрессом/отменой,
      по образцу `Threads.Overview.pas`.
- [ ] GUI-диалог: путь к видео эксперимента, offset (сек, ±), путь вывода,
      параметры полосы (позиция/высота/прозрачность/цвета каналов).
- [ ] Проверить на реальном видео и WAV; зафиксировать метод определения
      offset (по опорному событию) в `README.md`/`HISTORY.md`.
- [ ] Regression/smoke-проверка: короткий синтетический видеофрагмент +
      синтетический WAV с известным offset.
- [ ] Первая версия стиля полосы — точная копия текущего вида обзорного
      графика (белый фон, цвета каналов как сейчас), плюс подпись текущего
      времени WAV (ЧЧ:ММ:СС.ммм или секунды) поверх кадра. Настройка
      внешнего вида (позиция/прозрачность/фон/шрифт) — отдельным пунктом
      позже, не в первой версии.
- [ ] `Video.FfmpegLocate.pas`: поиск `ffmpeg.exe` рядом с exe → если не
      найден, диалог выбора файла → путь сохраняется в настройках (JSON,
      по аналогии с `Core.ConfigStore.pas`). Перед каждым запуском повторно
      проверять доступность сохранённого пути; если файл недоступен —
      повторить поиск рядом с exe, затем снова спросить пользователя.
- [ ] `ffprobe`/`ffmpeg -i` детект параметров видео эксперимента:
      разрешение, fps, длительность, наличие аудиодорожки — выходное видео
      рендерится под эти же разрешение/fps.
- [ ] Этап (A): headless-рендер кадров полосы в разрешении/fps видео
      эксперимента, поток кадров передаётся в ffmpeg (pipe, `rawvideo`)
      без промежуточных PNG; кодирование в контейнер с альфа-каналом
      (ProRes 4444 / WebM+VP9 alpha — выбрать по доступности кодека в
      конкретной сборке ffmpeg).
- [ ] Этап (B): ffmpeg `overlay` — видео эксперимента + видео полосы,
      сохранение аудиодорожки видео эксперимента как есть (замена/добавление
      дорожки из WAV — не в первой версии).
- [ ] Режим Preview: рендер короткого фрагмента (первые N секунд от
      выбранного Offset) для быстрой проверки совпадения — прогоняет оба
      этапа на укороченном диапазоне.
- [ ] Режим Full: полноразмерный рендер после подтверждения превью.
- [ ] Фоновый worker (`Threads.*`) для обоих этапов с прогрессом/отменой,
      по образцу `Threads.Overview.pas`.
- [ ] GUI-диалог (`GUI.VideoExportForm.pas`): выбор видео эксперимента,
      поле Offset (сек, ±) с кнопками Preview/Full, путь вывода, отображение
      обнаруженных параметров исходного видео.
- [ ] Проверить на реальном видео и WAV; зафиксировать процедуру подбора
      Offset в `README.md`.
- [ ] Позже (не в этой итерации): настраиваемые выходные параметры
      (разрешение/fps отличные от исходника), настраиваемый стиль полосы,
      подстановка аудиодорожки из WAV, пакетная обработка нескольких видео.


---

# Выполнено

- `[x]` Разделение `GUI.Plot.pas` на `GUI.Plot.Base.pas` + `GUI.Plot.Signal.pas` + `GUI.Plot.Overview.pas` выполнено 2026-09-19; подробности в `HISTORY.md`.
- `[x]` Переименование модулей выполнено 2026-09-12; подробности в `HISTORY.md`.
- `[x]` Оптимизация `FindPeaksProminence` до O(N) выполнена; подробности в `HISTORY.md`.
- `[x]` Исправление FIR15 padding и удаления занулённых крайних отсчётов выполнено; требуется golden test.
- `[x]` Исправлен off-by-one в `Detection.Correlation.pas`; regression test ещё не добавлен.
- `[x]` Восстановлено чтение позиций пиков для `dmPeakFile` в `GUI.Model.pas`.
- `[x]` Автопоиск парного WAV и стандартное имя `.eodpk` реализованы; подробности в `HISTORY.md`.
- `[x]` Открытие WAV и построение Overview для больших WAV переведены на фоновые workers с прогрессом и отменой.
- `[x]` Построение Overview для больших EODPK переведено на фоновый worker с прогрессом и отменой.
