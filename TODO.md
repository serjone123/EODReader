# TODO.md — план развития EODReader

Этот файл — рабочий план проекта. Все пункты имеют приоритет и статус.
После выполнения пункт переносится в раздел **Выполнено** в конце файла с краткой пометкой о результате. Если выполнение частичное — пункт остаётся в активном разделе, а прогресс фиксируется в `HISTORY.md`.

Статусы: `[ ]` не начато, `[~]` в работе, `[x]` выполнено.

## Правила работы для ИИ-агентов

Перед изменением кода агент обязан прочитать:

1. `README.md` — архитектура, форматы и общие сведения.
2. `AGENTS.md` — обязательные правила работы с проектом.
3. `TODO.md` — текущий план и уже выполненные пункты.
4. `HISTORY.md` — историю существенных изменений.

После существенного изменения агент должен добавить запись в `HISTORY.md`: что изменено, зачем, какие части логики/расчётов затронуты и как проверено. Особенно обязательно это для изменений математических расчётов, алгоритмов обнаружения/классификации и форматов данных.

---

## P0 — стабилизация и фиксация семантики

- [x] Исправить off-by-one в `Correlation`: последний допустимый старт шаблона проверяется (`0..N-L`). Добавлен кодовый фикс; regression test ещё нужен.
- [ ] **Определить полярность корреляции**: сейчас используется `Abs(correlation)`. Явно решить, нужна ли инвариантность к знаку; режим корреляции сделать частью конфигурации/логики и покрыть тестом.
- [ ] **Зафиксировать семантику позиции события**: определить, является ли позицией события позиция найденного пика или позиция максимума корреляции шаблона. Не оставлять это неявным побочным эффектом классификатора.
- [ ] **Определить правило классификации**: один лучший тип на кандидата или несколько типов при превышении порога. Предпочтительно перейти к одному лучшему совпадению с сохранением score/confidence.
- [ ] **Переработать подавление близких событий**: вместо сохранения первого события использовать non-maximum suppression/группировку и оставлять наиболее качественный кандидат по score/prominence и другим признакам.
- [ ] **Исправить `TEodGuiSession.GetPeakPositions` для `dmPeakFile`**: сейчас путь через `LoadPeakPositions` фактически не возвращает позиции из-за оставшегося закомментированного кода.
- [ ] **Убрать опасное сужение `Int64 -> Integer`** для количества пиков; API хранения должен поддерживать большие файлы.
- [ ] **Усилить валидацию WAV**: проверить `BlockAlign`, границы chunk/data относительно размера файла, параметры `WAVE_FORMAT_EXTENSIBLE`, совместимость двух входных WAV.
- [ ] **Удалить устаревший закомментированный код** из `GuiModel` и других модулей, если он больше не нужен. История Git сохраняет старые реализации.

## P0 — тестируемость и математическая корректность

- [ ] Создать набор unit/regression-тестов для `WavReader`, `Statistics`, `Fir15`, `Peaks`, `Correlation`, `Classifier`, `PeakStore`.
- [ ] Создать **golden test для FIR15** и сравнить Delphi-реализацию с эталоном MATLAB: максимум и RMS ошибки должны быть заданы явно.
- [ ] Создать end-to-end тест с эталонными результатами: входная запись → ожидаемые позиции событий, типы, каналы и допустимые погрешности.
- [ ] Зафиксировать в документации математическую модель детектора: почему используется STD по четырём каналам, какие единицы у порогов и как интерпретируются признаки.

## P1 — явная модель конвейера обработки

- [ ] Ввести отдельные понятия/типы для этапов: `CandidatePeak`, `Classification`, `Event`.
- [ ] Сделать явным конвейер: `RawSignal → FeatureSignal → CandidatePeaks → ClassifiedEvents → DeduplicatedEvents`.
- [ ] Развести данные пика и данные классификации: score, тип, канал, позиция совпадения и confidence не должны теряться или смешиваться.
- [ ] Определить единую стратегию сортировки и дедупликации событий.

## P1 — работа с длинными записями

- [ ] Перевести `AnalyzePeaks` с полной аллокации `AllFiltered` на существующий `TFloatSignalCache` и потоковую/чанковую обработку.
- [ ] Добиться потребления RAM, не зависящего линейно от длительности записи.
- [ ] Добавить в `SignalCache` заголовок с версией, sample rate, числом сэмплов, идентификатором исходника и параметрами обработки/версией алгоритма.
- [ ] Проверить корректность чтения границ сегментов при переходе между чанками.

## P1 — конфигурация и воспроизводимость

- [ ] Перевести окна `WindowBefore/After`, `ExtractionBefore/After` и связанные параметры из неявных sample units в физические единицы (например, ms), если это не ломает совместимость; преобразование в samples выполнять внутри алгоритма.
- [ ] Версионировать конфигурацию детектора и версию алгоритмов.
- [ ] Не скрывать повреждённый `config.json`: хотя бы логировать/показывать причину отката к значениям по умолчанию.
- [ ] Сделать результаты анализа воспроизводимыми по исходнику + конфигурации + версии алгоритмов.

## P2 — улучшение качества обнаружения и классификации

- [ ] Исследовать адаптивный порог на основе median/MAD вместо одного фиксированного `PeakProminence`.
- [ ] Добавить дополнительные признаки: RMS, амплитуда, prominence, длительность, rise/fall, энергия, отношения каналов.
- [ ] Ввести понятие confidence/quality score и сохранять его в результате.
- [ ] Пересмотреть hardcoded шаблоны: вынести шаблоны в версионируемые внешние файлы (например, отдельный формат шаблонов), а не хранить их в Pascal-коде.
- [ ] Отдельно решить судьбу `Stim`: текущий шаблон выглядит синтетическим; определить, является ли он временной заглушкой или реальным эталоном.

## P2 — формат результатов

- [ ] Развить `.eodpk` из хранилища пиков в воспроизводимый формат результатов анализа: версия детектора, версия шаблонов, конфигурация, события, confidence и при необходимости дополнительные признаки.
- [ ] Сохранить совместимость с существующим `.eodpk` v3 или реализовать явную миграцию/новую версию формата.

## P2 — производительность

- [x] Перевести `FindPeaksProminence` с O(N²) на O(N) двумя проходами монотонного стека; семантика заявлена как сохранённая. Нужны regression-тесты на совпадение со старой реализацией.
- [ ] Измерить стоимость template correlation и только после профилирования рассматривать sliding dot product/prefix sums или FFT.
- [ ] Ограничить частоту UI progress updates (например, десятками обновлений в секунду), не вызывая синхронизацию главного потока на каждый чанк.
- [ ] Сделать отмену отзывчивой внутри длительных операций поиска пиков и корреляции, а не только между крупными чанками.

## P2 — очистка и развитие GUI

- [ ] После стабилизации ядра упростить `GuiModel` и убрать совместимость с устаревшей архитектурой.
- [ ] Убедиться, что GUI использует единый API событий независимо от источника (`WAV` / `.eodpk`).
- [ ] Не смешивать изменения GUI с изменениями математической логики в одном коммите без необходимости.

---

# Выполнено

- `[x]` Переименование модулей выполнено 2026-09-12; все `.pas` сохранены в одной папке, имена отражают подсистемы. Подробности: `HISTORY.md`.
- `[x]` Оптимизация `FindPeaksProminence` до O(N) выполнена; подробности и ограничения зафиксированы в `HISTORY.md`.
- `[x]` Исправление FIR15 padding и удаления занулённых крайних отсчётов из чанков выполнено; требуется отдельная проверка на стыках чанков и golden test.
- `[x]` Исправлен off-by-one в `Detection.Correlation.pas`; regression test ещё не добавлен.
